{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认主窗口能力：允许前端通过全局 __TAURI__ 调用核心命令、读取窗口与拖放事件",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default", "opener:allow-open-url"]
}